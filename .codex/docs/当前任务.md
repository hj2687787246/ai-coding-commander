# 当前任务

- 当前任务名称：执行失败复发闭环
- 当前任务目标：让 Codex 在命令、工具、安装、测试、构建、git、shell 或环境查找失败后，把已验证可用方法变成后续同类操作默认路径，再由复用沉淀流程决定是否落盘。
- 当前任务模式：实现
- 当前任务形状：single
- 执行强度：compact
- 当前进度：已完成：新增 verify_skill_install.py 安装自检，覆盖三个正式 skill 的 frontmatter、SKILL.md 和 bundled files 一致性；真实 live 自检 ok=true
- 当前卡点：无
- 正在关注的文件：skills/commander-mode/scripts/verify_skill_install.py, skills/commander-mode/SKILL.md, README.md, install/install-commander.ps1, tests/test_verify_skill_install.py
- 验证状态：已验证
- 验证证据：python -m pytest -q: 78 passed；ruff check tests, verify_skill_install.py and known_failures.py: passed；git diff --check: exit 0；path scan: no user absolute path leaks；verify_skill_install.py 对真实 live 返回 ok=true
- 下一步：无；等待下一项明确任务
- 最近验证：2026-05-07 pytest 78 passed；ruff check tests, verify_skill_install.py and known_failures.py passed；git diff --check exit 0；live install verifier ok=true
