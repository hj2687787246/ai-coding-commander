# Agent repo commander runtime archive

This directory archives the former `D:\Develop\Python-Project\Agent\commander` runtime and its `tests/test_commander*.py` test surface.

It was moved out of the Agent learning/project repository so that the Agent repo can focus on the job-search learning line: FastAPI, RAG, Agent, LangChain, LangGraph, async/concurrency, evaluation, and deployment.

Status:

1. Preserve for reference; do not treat as the current learning task.
2. Reuse only if a future project or interview demo needs a concrete runtime/stop-gate/multi-agent example.
3. Prefer the lightweight `skills/commander-mode` portable harness for cross-repository usage.
