# skill-source 说明

这个目录保存当前 Agent 仓库自己的项目 skill 真相源。

通用 `commander-mode` 已经外置到：

- `D:\Develop\Python-Project\ai-coding-commander\skills\commander-mode`

本机安装入口现在应指向外部仓库：

- `C:\Users\26877\.codex\skills\commander-mode`

当前包含：

1. `agent-workspace/`：当前 Agent 仓库的项目 adapter，详细项目规则放在 `references/` 下。
2. `commander-reuse-upgrader/`

当前仓库不再维护通用 `commander-mode` 的源码副本，避免学习项目继续承担通用工具仓库职责。
